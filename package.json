{
  "name": "fabric-production-tracker",
  "version": "1.0.0",
  "description": "Fabric production tracking system with QR code generation",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd client && npm start",
    "dev:full": "concurrently \"npm run dev\" \"npm run client\"",
    "heroku-postbuild": "npm run build",
    "build": "node build-for-render.js",
    "build:fallback": "echo 'Starting fallback build...' && cd client && echo 'Installing client dependencies...' && npm install --production=false && echo 'Building React app...' && npm run build && echo 'Build completed successfully!'",
    "start:prod": "NODE_ENV=production npm start",
    "test:prod": "npm run build && npm run start:prod"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "firebase-admin": "^13.4.0",
    "multer": "^2.0.1",
    "path": "^0.12.7",
    "qrcode": "^1.5.3"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": "18.x"
  }
}
